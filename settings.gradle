pluginManagement {
    repositories {
        maven { url "https://maven.fabricmc.net/" }
        maven { url "https://maven.architectury.dev/" }
        maven { url "https://maven.neoforged.net/releases/" }
        gradlePluginPortal()
    }
}

if (JavaVersion.current().ordinal() + 1 < 25) {
    throw new IllegalStateException("Minecraft 26.2 requires running Gradle with Java 25 or newer.")
}

include("common")
include("fabric")
include("neoforge")

rootProject.name = "MineTogether Servers"
